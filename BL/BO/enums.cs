
namespace BO
{

    public enum Priorities { Emergency = 0, Regular = 1, Fast = 2 };
    public enum WeightCategories { Lightweight = 0, medium = 1, heavy = 2 }
    public enum DroneStatus { available, maintenance, delivery }
    public enum ParcelStatus { defined, associated, collected, provided }
}
